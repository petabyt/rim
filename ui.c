struct nk_context {
	
};

int nk_button_label(struct nk_context *ctx, const char *title) {
	/*
	1. Append item to tree in current pos
	2. Check widget event queue
	  - If tree pos == current tree pos: return 1
	*/
	return 0;
}
